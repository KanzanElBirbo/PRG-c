#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
int x;
int y;


scanf("%d %d", &x, &y);
if(x > 0) {
    x *= -1;
}
return x;
}

sum = x + y; {
printf("%i", &sum);
}
